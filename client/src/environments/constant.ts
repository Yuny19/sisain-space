import { environment } from './environment';

export const constant = {
    productURL: `${environment.baseUrl}/product`,
    transactionURL: `${environment.baseUrl}/transaction`,
    userURL: `${environment.baseUrl}/user`,
    expeditionURL: `${environment.baseUrl}/expedition`,
    orderURL: `${environment.baseUrl}/order`,
    addressURL: `${environment.baseUrl}/address`
}